.span12
  .divider-space
  .module-header.speakers-header
    %h4= t('team.header')
.span12.hero-unit.text-center.white
  %h2= t('team.people_that_make')
.span12
  .divider-space
.span12
  .carouselslider.speakers-carousel.item-4
    - @team.each_slice(3) do |slice|
      .span12
        %ul.people
          - slice.each do |speaker|
            = render partial: 'team/member', locals: {speaker: speaker}
